/* global expect, it, inject, beforeEach, describe */
(function()
{
	"use strict";

	describe("Angular Minimodal", function()
	{
		describe(".show()", function()
		{
			it("fails when not providing an options object", function()
			{
				expect(true).toBe(true);
			});

			it("fails when not providing a templateUrl on options object", function()
			{
				expect(true).toBe(true);
			});

			it("fails when not providing a controller on options object", function()
			{
				expect(true).toBe(true);
			});

			it("returns a modalInstance when successfully showing a popup", function()
			{
				expect(true).toBe(true);
			});
		});

		describe("modalInstance", function()
		{
			it("has a resolve method", function()
			{
				expect(true).toBe(true);
			});

			it("has a reject method", function()
			{
				expect(true).toBe(true);
			});
		});
	});
}());